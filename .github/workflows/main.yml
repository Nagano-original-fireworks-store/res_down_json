name: pull_json_data

on:
  schedule:
    - cron: '0 1 * * *'  # 每天凌晨1点执行
  push:
     branches:
        - main
  
jobs:
  
  build:
    runs-on: ubuntu-latest
    env:
      version: "4.3.0"
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: Run os region decode
      run: |
        cd tools
        chmod +x region_decode
        ./region_decode "OSRELAndroid4.3.0" "3cf6034ee8f9c9fa" "5" "OS" "$version" "https://osasiadispatch.yuanshen.com/query_cur_region"
    - name: Run cn region decode
      run: |
        cd tools
        chmod +x region_decode
        ./region_decode "CNRELAndroid4.3.0" "a1bec5d0a9c892a4" "4" "CN" "$version" "https://cngfdispatch.yuanshen.com/query_cur_region"
    - name: Run region conve
      run: |
        cd tools
        chmod +x convert_region
        ./convert_region
    - name: Upload output
      uses: actions/upload-artifact@v2
      with:
        name: "JSON"
        path: tools/output
